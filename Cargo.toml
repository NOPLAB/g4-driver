[workspace]
members = ["bldc", "bldc-sim", "firmware", "controller", "protocol"]
resolver = "2"

# Firmware build profiles (shared for workspace)
[profile.dev]
debug = true
lto = true
opt-level = "z"
incremental = true

[profile.release]
debug = false
lto = true
opt-level = 2
incremental = true
